# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
dist-electron/
out/
build/output/
release/
*.exe
*.msi
*.nsis

# Vite
.vite/

# Local databases (do not commit user data)
*.sqlite
*.sqlite3
*.db
data/
.electron-dev/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Cursor local tooling (never commit)
.cursor/

# Local-only activation key for packaged installs you build (not CI / public releases)
resources/local-activation-key.txt

# Test / coverage
coverage/
.nyc_output/

# Local design scratch — sketches, mockups, notes. NEVER committed/uploaded.
sandbox/

# Branding assets kept local only (logos, colors, mockups, source files).
branding/

# Demo seed bundle (built locally from dev data — never commit)
resources/demo/

# Local-only agent context file (not shared/uploaded).
AGENTS.md

# Misc
*.tsbuildinfo

# Local reference PDFs (not part of the app)
CANULC-S536.pdf

# One-off local seed / OCR helpers that may contain client PII — never push
scripts/seed-adelaide.mjs
scripts/write-adelaide-gaps-pdf.mjs
data/parse-adelaide-devices.py

