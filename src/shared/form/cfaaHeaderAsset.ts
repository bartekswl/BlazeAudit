import cfaaMemberLogoDataUrl from './imagesCFAA.jpeg?inline';

/**
 * Bundled as an inline data URL so template preview, documents, packaged
 * installs, and Chromium PDF export all use the exact same offline asset.
 */
export const CFAA_MEMBER_LOGO_DATA_URL = cfaaMemberLogoDataUrl;
